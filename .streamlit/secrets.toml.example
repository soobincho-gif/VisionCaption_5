# Optional for upload/caption/embed features in the Streamlit app.
# Root-level secrets are exposed as environment variables in Streamlit,
# so this maps directly to settings.openai_api_key / OPENAI_API_KEY.
OPENAI_API_KEY = "sk-your-openai-key"
